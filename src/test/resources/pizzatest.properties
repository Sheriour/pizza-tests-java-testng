# Either "local" when running on a local machine or "remote" when running with selenium grid
runmode=remote

# Between 1 and 3. Values above 1 automatically enable parallel runs
threadcount=3

# Flag to enable failed test retries
retryFailedTests=true
# How many times to retry failed tests. Will be ignored if retryFailedTests=false
retryTestCount=1